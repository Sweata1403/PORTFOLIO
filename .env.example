PORT=4000
CLIENT_ORIGIN=http://localhost:5173
SESSION_TTL_HOURS=168
SESSION_COOKIE_NAME=sweata_session
# Optional seed asset overrides. By default, bundled files in server/seed-assets are used:
# SEED_PROFILE_IMAGE_SOURCE=/absolute/path/to/profile.jpeg
# SEED_RESUME_SOURCE=/absolute/path/to/resume.pdf
# In production, set this before using the one-time setup form:
SETUP_KEY=140302
